# ── ASR (학습된 Whisper 추론) ─────────────────────────────
transformers>=4.39.0
torch>=2.2.0
torchaudio>=2.2.0
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.26.0

# ── UI ────────────────────────────────────────────────────
streamlit>=1.33.0
streamlit-mic-recorder>=0.0.8

# ── 메뉴 매칭 (한국어 형태소 + fuzzy + 의미 임베딩) ────────
kiwipiepy>=0.17.0
python-Levenshtein>=0.25.0
sentence-transformers>=2.6.0

# ── LLM (챗봇) ────────────────────────────────────────────
anthropic>=0.50.0

# ── TTS (선택, 음성 안내) ─────────────────────────────────
gTTS>=2.5.0

# ── 테스트 ────────────────────────────────────────────────
pytest>=8.0.0
